# JavaScript scaffold for coding challenge

Inital stubs for challenge answers are named `challenge-*.js`.

Initial tests are named `challenge-*.test.js`.

How to set up:
 * pnpm i

How to run:
 * Once: `pnpm test`
 * Watch mode: `pnpm test -- --watch`
